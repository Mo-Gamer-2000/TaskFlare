// Define the Public component as a functional component
const Public = () => {
  // Render the Public component UI
  return (
    <div>
      {/* Display the title with a purple accent */}
      <h1 className="text-purple-accent">Public</h1>
    </div>
  );
};

// Export the Public component as the default export
export default Public;
